# QuickSlot 規格書

> 一個輕量、config 驅動的本地 PixiJS slot demo 框架。
> 用途：拿到比賽題目後，**用 AI 在最短時間內產出一個本地可玩的 slot 試玩**。
> 版本：v0.1（規劃）｜日期：2026-06-03

---

## 0. 一句話定位

**不是 Stake 架構、不接 RGS、不做合規。** 只要在本機 `npm run dev` 就能玩、看起來有模有樣、能在比賽 demo 的最小框架。核心做成**玩法無關**，payline 先跑通，tumble 設計成**可插拔的一層**。

---

## 1. 目標與非目標

### 要做
- 一個可重複使用的 **vanilla PixiJS 8 + Vite + TypeScript** 模板（不用 Svelte、不用 monorepo）。
- 本地數學引擎：在瀏覽器/Node 內算 spin 結果，**取代整個 RGS round-trip**。
- config 驅動：換一個遊戲只改 `game.config.json` + 換 `assets/`。
- 一條清楚的 **AI workflow**：題目 → 美術 → 數學 → 整合 → 可玩 demo，每步都標明用什麼 skill、怎麼驗證。
- 玩法無關核心 + payline 先行 + tumble/cluster 之後可插拔升級。

### 不做（避免 feature creep）
- 不接 Stake RGS / `/wallet/play` / BookEvent 合約。
- 不做 i18n、合規 disclaimer、送審 checklist 那套。
- 不做帳號 / 錢包 / 後端。balance 純前端記憶體變數。
- 不追求數學精算到上線等級，RTP 只需「sanity 合理」。

---

## 2. 借鏡與切割（從現有資源拿什麼、丟什麼）

| 來源 | 拿什麼 | 丟什麼 |
|---|---|---|
| **StakeProject / web-sdk** | reel 轉軸動畫的**概念**、`evaluate_lines/ways/cluster` 的**算法邏輯**、config/paytable 的**資料形狀**、tumble 編排的**踩坑教訓** | 整個 package（Svelte、RGS fetcher、BrandUI、i18n、BookEvent 合約）——太重，不搬 |
| **StakeProject / CLAUDE.md** | P0 教訓：`broadcastAsync 必須 await`、`waitForResolve 配 timeout`、build cache 要清、tumble 迴圈會卡死 | — |
| **SpineAI / codex-imagegen** | **整套直接用**（產透明 PNG 符號、背景、logo），已成熟 | — |
| **SpineAI / spineai-patterns + gen-spine** | 1–2 個 hero FX（wild/scatter 脈動、旋轉）；**省著用**（半成品） | 整套角色動畫、constraint 動畫（未完成） |
| **AI_team_start_template** | **knowledge-graph MCP**：跨次累積做 slot 的踩坑與套路 | agent-bridge 多 agent 團隊——對「快速出 demo」過度工程，比賽期間先不用 |

---

## 3. 架構總覽

三層，核心與玩法解耦：

```
┌─────────────────────────────────────────────┐
│  UI 層 (HTML overlay)                         │
│  spin / bet / balance / paytable modal        │
├─────────────────────────────────────────────┤
│  Render 層 (PixiJS 8)                          │
│  Reels · Symbol · WinPresenter · [TumbleLayer] │  ← TumbleLayer 可插拔
│            eventPlayer (handler map)           │
├─────────────────────────────────────────────┤
│  Core 層 (純 TS，玩法無關)                      │
│  rng → engine.spin() → evaluator → Event[]     │
│        evaluators/{payline|ways|cluster}       │  ← 依 config.mechanic 切換
└─────────────────────────────────────────────┘
         ▲ game.config.json + assets/ 驅動全部
```

### 關鍵設計：本地 Event 串流（取代 BookEvent）
`engine.spin()` 回傳一個 `SpinResult`，內含一串 `Event[]`，render 層用 handler map 逐一播成動畫。玩法差異**只體現在 evaluator 產出哪些 event**：

- **payline**：`reveal` → `win`(每條中獎線) → `totalWin`
- **cluster**：`reveal` →（`win`(群) → `tumble`(消除+補新)）迴圈 → `totalWin`

Render 層的 `tumble` handler 是獨立模組（`TumbleLayer`），payline 模式根本不會發 `tumble` event，所以**核心與 payline 完全不需要知道 tumble 存在**——這就是「可插拔」。

---

## 4. 檔案結構

```
QuickSlot/
├─ 規格書.md                    ← 本文件
├─ template/                    ← 可重複使用模板（init 時從這 clone）
│  ├─ package.json              （pixi.js@8、vite、typescript）
│  ├─ vite.config.ts
│  ├─ index.html
│  ├─ game.config.json          ← 每個遊戲主要只改這裡
│  ├─ public/assets/            ← codex 產的符號/背景/logo、spine 輸出
│  └─ src/
│     ├─ main.ts                啟動 PixiJS App、載資產、接 UI
│     ├─ core/                  ★ 玩法無關
│     │  ├─ types.ts            Config / SpinResult / Event 型別
│     │  ├─ rng.ts              可 seed 的亂數
│     │  ├─ engine.ts           spin() 編排器
│     │  └─ evaluators/
│     │     ├─ index.ts         依 config.mechanic 選 evaluator
│     │     ├─ payline.ts       ✅ 第一個實作
│     │     ├─ ways.ts          之後
│     │     └─ cluster.ts       之後（產 tumble events）
│     ├─ render/
│     │  ├─ Reels.ts            轉軸 + 停軸 bounce
│     │  ├─ Symbol.ts           符號 sprite（idle / 中獎脈動）
│     │  ├─ WinPresenter.ts     中獎高亮 + 連線 + 贏分 countup
│     │  ├─ TumbleLayer.ts      ★ 可插拔：爆破/下落/補新
│     │  └─ eventPlayer.ts      Event[] → 動畫（handler map，全程 await）
│     ├─ ui/Controls.ts         spin/bet/balance/paytable
│     └─ assets/manifest.ts     資產清單
└─ tools/
   ├─ sim.mjs                   本地跑 N 萬次 spin 估 RTP
   └─ gen-art.md                codex-imagegen 呼叫範本（符號/背景/logo prompt）
```

---

## 5. game.config.json schema（草案）

刻意沿用 StakeProject 的形狀，方便搬算法。

```jsonc
{
  "theme": "greek-gods",            // 主題（影響美術 prompt）
  "mechanic": "payline",            // payline | ways | cluster
  "grid": { "reels": 5, "rows": 3 },
  "paylines": 25,                   // payline 專用
  "bet": { "default": 1.0, "levels": [0.2, 0.5, 1, 2, 5] },
  "rtpTarget": 0.95,
  "symbols": [
    { "id": "H1", "kind": "high", "pay": { "3": 5, "4": 20, "5": 100 } },
    { "id": "L1", "kind": "low",  "pay": { "3": 1, "4": 3,  "5": 10 } },
    { "id": "W",  "kind": "wild" },
    { "id": "S",  "kind": "scatter", "pay": { "3": 2, "4": 10, "5": 50 } }
  ],
  "reelStrips": [ ["H1","L1","..."], ["..."], ... ],  // 每軸符號帶
  "freeSpins": { "trigger": 3, "count": 8 }           // 選配
}
```

---

## 6. ★ AI Workflow（核心）

從「拿到比賽題目」到「本地可玩 demo」。每階段標明**動作 / 用到的 skill 或工具 / 輸出 / 驗證方式**。

| # | 階段 | 動作 | Skill / 工具 | 輸出 | 怎麼驗證 |
|---|---|---|---|---|---|
| 1 | **定主題與玩法** | 依題目決定主題、玩法（先 payline）、符號清單、paytable 草案、RTP 目標 | 新建 `quick-slot-spec` skill（從 StakeProject `write-spec` 精簡而來，砍掉 Stake 欄位）｜可配 `knowledge-graph` 查過去類似題 | `game.config.json` 初版 + 主題美術 brief | config 通過 JSON schema 驗證；符號數、paytable、RTP 目標欄位齊全 |
| 2 | **scaffold 模板** | 從 `template/` clone 出新遊戲資料夾，注入 config | 新建 `init-quick-slot` skill（取代 Stake 的 `init-game`，因為那個綁 web-sdk） | 可跑的專案資料夾 | `npm install && npm run dev` 起得來；**空殼能按 spin、轉軸會動**（先用 placeholder 色塊） |
| 3 | **產數學 / 轉軸帶** | 依 paytable 生成 `reelStrips`，本地模擬估 RTP，調整符號權重 | `tools/sim.mjs`（本地 Node 模擬）｜借 StakeProject `optimize-rtp` 的**概念**（非那支 script） | 定版 `reelStrips` + RTP 報告 | 跑 ≥100k 次 spin，RTP 落在 `rtpTarget ± 3%`；hit frequency 合理（不會整場不中或每轉必中） |
| 4 | **產符號美術** | 依主題批次產符號、背景、logo（透明 PNG）；**派多個 subagent 平行呼叫 codex** | **SpineAI `codex-imagegen`**（直接用，subagent 內呼叫）；prompt 範本見 `tools/gen-art.md` | `public/assets/` 內所有符號圖 | 每張：RGBA 透明背景、尺寸一致、非空白（≥1% 不透明像素）；人工挑 variant 確認風格一致 |
| 5 | **（選配）hero FX** | 為 wild / scatter / big win 做 1–2 個 Spine 動畫（脈動、旋轉、發光） | **SpineAI `spineai-patterns` + gen-spine**（省著用，半成品）｜不行就退回 PixiJS 程式化 tween/particle | `.json + .atlas + .png` spine 檔 | 用 pixi-spine 載入不報錯、動畫能播；遵守 T01 `pma:false`、T07 不設 default attachment、T08 set default animation |
| 6 | **整合與調動畫** | 接資產、寫 evaluator（payline）、串 eventPlayer、調轉軸/中獎/countup 手感 | 框架自身（Read/Edit/Write）｜借 StakeProject `add-event` 概念加新 event handler | 完整可玩 payline demo | 完整流程順跑：spin → 停軸 → 中獎高亮 → 連線 → countup → 可再轉；**所有 await 串好不卡 round**；direct 視窗縮放不爆版 |
| 7 | **（升級）掛 tumble / cluster** | 需要更炫時：實作 `cluster.ts` evaluator + 啟用 `TumbleLayer` | 框架自身 | cluster + tumble demo | 連鎖迴圈正確：消除→下落→補新→重判→停；**每步 broadcastAsync 必 await、waitForResolve 配 timeout**；連續多次 tumble 不卡死 |
| 8 | **打磨與驗收** | 對照驗收清單做最後檢查 | 新建 `quick-slot-review` skill（從 Stake `audit-launch` 砍掉合規/i18n，只留功能與手感） | 驗收報告 | 見 §7 驗收標準逐項過 |
| 9 | **記錄到 KG** | 把這次踩的坑、有效的 prompt、調 RTP 的數據存進知識圖 | **`knowledge-graph` MCP**（store_knowledge / connect_knowledge）｜借 Stake `lesson` 概念 | KG 節點 | 下次同類題目能 `search_memory` 找回 |

### 需要新建的 skill（3 個）
StakeProject 的 `init-game` / `audit-launch` 綁死 web-sdk 與 Stake 合規，**不能直接重用**。所以本框架要新建三個輕量 skill（用 `skill-creator` 做）：

1. **`quick-slot-spec`** — 問答式產 `game.config.json` + 美術 brief。
2. **`init-quick-slot`** — 從 `template/` scaffold 新遊戲、注入 config、跑起 placeholder 可玩空殼。
3. **`quick-slot-review`** — 功能與手感驗收 checklist（非合規）。

直接重用的 skill：`codex-imagegen`、`spineai-patterns`/`gen-spine`、`knowledge-graph`、`skill-creator`。

---

## 7. 驗證策略與驗收標準

### 分階段驗證（已嵌在 §6 每列「怎麼驗證」）
原則：**每個階段都有可機器檢查或可肉眼確認的關卡**，不靠猜。

### 最終驗收標準（demo 可交付）
- [ ] `npm run dev` 一次成功，無 console error。
- [ ] 能按 spin、轉軸動畫流暢、能停。
- [ ] 中獎時有高亮 + 連線（payline）+ 贏分 countup，金額正確（與 evaluator 回傳一致）。
- [ ] balance 正確扣 bet、加 win。
- [ ] paytable modal 開得起來、內容對得上 config。
- [ ] 連續快速點 spin 不會卡住 / 不會 race（所有動畫 await 串好）。
- [ ] 視窗縮放 / 直式不爆版。
- [ ] 美術風格一致、看起來像個成品（非色塊）。
- [ ] `tools/sim.mjs` 證明 RTP 落在目標附近。

### 高風險點（來自 StakeProject 實戰教訓）
- **tumble 迴圈卡死** — `broadcastAsync` 一律 `await`，`waitForResolve` 配 `Promise.race([..., timeout(2000)])`。這是 cluster 升級時最容易出事的地方。
- **build cache 殘留** — 改完資產/設定若行為沒變，先刪 `dist/` 再 build。
- **codex imagegen 陷阱** — `CODEX_TIMEOUT` 別低於 250s；不要在 prompt 要求精確數字/文字（會漂移，數字用 Spine slot 疊）；暫存目錄別放 Temp（ACL 問題）。
- **spine 半成品** — 只做最簡單的 FX；不行就退回 PixiJS tween，不要卡在 spine。

---

## 8. 里程碑

| 里程碑 | 內容 | 完成定義 |
|---|---|---|
| **M0 骨架** | template 結構、core 型別、PixiJS 起得來、placeholder 色塊能 spin | 空殼可轉 |
| **M1 payline 可玩** | payline evaluator + 中獎呈現 + countup + UI + sim | 用 placeholder 圖完整可玩，RTP 合理 |
| **M2 美術** | codex 產符號/背景/logo，替換 placeholder | 看起來像成品 |
| **M3 升級（選配）** | cluster evaluator + TumbleLayer，或 ways | 連鎖/多 ways 正確不卡 |
| **M4 打磨** | hero FX、音效、手感、驗收 | 過 §7 驗收清單 |

> 比賽流程：拿到題目後，跑 workflow §6 步驟 1→2→3→4→6→8，通常 M2 就足以 demo；時間夠再 M3/M4。

---

## 9. 已定決策

1. **音效**：初始階段先省略。在 §1 定主題時與使用者討論需要哪些音效，把**音效規格**（清單 + 用途 + 觸發時機）寫進該遊戲的 spec，使用者之後自行找素材。真正實作時用 **`@pixi/sound`**（PixiJS 官方套件，比 Howler 少一個相依；Howler 是 web-sdk 舊選擇，不採用）。
2. **版面**：以**橫式（landscape）**為主要 layout。
3. **版型**：採本文件自訂格式（不對齊 `/dual_plan`）。
4. **協作模式**：**不用 agent-bridge 多 agent 團隊**（對此規模過度工程）。改用 Claude Code 內建 subagent：
   - **前端** = 主線（Claude Code 本身）。
   - **數學** = 內聯腳本 `tools/sim.mjs`，不開 agent。
   - **美術** = 派 subagent **平行**呼叫 codex 產圖（單張 150–300s，平行才划算）。
   - 跨次記憶仍用 **knowledge-graph MCP**。

### 9.1 音效規格範本（定主題後填，交給使用者找素材）

| 事件 | 類型 | 觸發時機 | 備註 |
|---|---|---|---|
| `bgm_base` | BGM | 進遊戲循環播放 | loop |
| `sfx_spin` | SFX | 按下 spin | |
| `sfx_reel_stop` | SFX | 每軸停下 | 可 5 軸各一或共用 |
| `sfx_win_small` | SFX | 一般中獎 | |
| `sfx_win_big` | SFX | 大獎 / countup | |
| `sfx_scatter` | SFX | scatter 落定 | |
| `bgm_freespin` | BGM | 進免費遊戲 | 選配 |

> 實作前此表先空著；定主題時依玩法增減，填好觸發時機後交給使用者備齊 `.mp3/.ogg`。
```
