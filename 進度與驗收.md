# QuickSlot — 進度與驗收（2026-06-03）

## 目前狀態

M0 骨架 + M1 payline 可玩（前次已驗證）。**本次再補完模板架構**:

- **M3 玩法全做進模板**:`ways` + `cluster`(含 `tumble` 連鎖)evaluator,玩法由 `game.config.json` 的 `mechanic` 切換。**核心 `engine` 與 `payline` 零回歸**。
- **美術 drop-in 路徑**:符號 `Sprite` 化 + `manifest.json` 驅動——丟 PNG + 寫 manifest 即自動 Sprite 化(背景/ logo 同理),**程式免改**;無美術自動 fallback 色塊、零 404。
- **音效 scaffold**:`@pixi/sound` 的 `SoundManager`(manifest 驅動、無音檔全程靜音零噪音)、reel-stop 解耦掛點、🔊 靜音鈕。
- **skill 套件 3 → 10**:新增 `tune-rtp` / `gen-slot-art` / `add-mechanic` / `add-event` / `gen-slot-fx` / `add-sound` / `kg-log`。
- **Knowledge Graph**:專案層 MCP 整進 repo(`.mcp.json` + `mcp/knowledge-graph/`),**不裝全域、不裝 agent 團隊**(見 `docs/knowledge-graph.md`)。
- **版控**:本次 `git init` 起,逐小步 commit;`.gitignore` / `.gitattributes`(LF 統一)齊備。

## 怎麼跑

```bash
cd Desktop/QuickSlot/template
npm install
npm run dev          # http://localhost:5173,按 SPIN 或空白鍵
```

估 RTP（前端與此共用同一份 core,數學一致）:

```bash
cd Desktop/QuickSlot
node tools/sim.mjs 500000
```

切玩法:改 `template/game.config.json` 的 `mechanic`(`payline`|`ways`|`cluster`),cluster 另設 `minCluster`。切後用 `tune-rtp` 重新對 RTP。

## 本次驗證（自動,已跑）

| 驗證 | 結果 |
|---|---|
| `npx tsc --noEmit`(strict) | ✅ 通過 |
| `npx vite build` | ✅ 成功(bundle 335KB gzip 105KB,含 @pixi/sound) |
| payline 200k sim | ✅ RTP **94.4%**、命中 48.7%(與 M1 一致,零回歸) |
| ways 100k sim(覆寫 mechanic) | ✅ 有限、命中 50.5%(RTP 306% 待 tune-rtp 重對) |
| cluster 50k sim(覆寫 mechanic) | ✅ 迴圈皆終止、tumble 盤面 **0 異常**(RTP 1716% 待重對) |
| `game.config.json` JSON 解析 + 符號數 | ✅ 10 個符號、5 軸轉軸帶 |

> ways/cluster 的高 RTP 是**沿用 payline paytable** 的預期結果(量級不同);要當主玩法時用 `tune-rtp` 線性重對。

## 還剩(需你做)

- **KG 啟用**:`cd mcp/knowledge-graph && npm install` → 重啟 Claude Code(原生套件編譯需你授權;見 `docs/knowledge-graph.md`)。
- **M2 美術**:用 `gen-slot-art` skill 產 Olympus 圖(本次依你決定**延後**;drop-in 路徑已就緒,丟圖 + manifest 即生效)。
- **手感人工驗收**:開 dev 點 SPIN——連點不卡、tumble 連鎖順、靜音鈕有效、視窗縮放不爆版——需真人在瀏覽器確認(對照 `quick-slot-review`)。

## 架構（已落實,與規格書一致）

- **玩法無關核心** `src/core/`(純 JS,前端與 sim 共用):`rng` / `engine` / `evaluators/{payline,ways,cluster}` / `paylines`。`engine.spin()` 把 `rng` 傳給 evaluator(payline/ways 忽略;cluster 用於 tumble 補新)。
- **PixiJS 渲染** `src/render/`:`reels`(錯位停軸+回彈+tumble 下落補新)、`winPresenter`(高亮+連線+countup;ways/cluster 的 `line<0` 只高亮不連線)、`TumbleLayer`(★可插拔 burst FX)、`sound`(SoundManager)、`eventPlayer`(逐事件**全程 await**)。
- **資產 drop-in** `src/assets/manifest.ts`:`public/assets/manifest.json` 為開關;`public/assets/audio/manifest.json` 為音效開關。
- **HTML UI** `src/ui/controls.ts`:押注 / 餘額 / SPIN / 賠付表;`index.html` 另有靜音鈕。
- **config 驅動** `game.config.json`:換遊戲主要只改這裡。

## 已知限制

- 符號仍是 placeholder 色塊(美術 drop-in 已就緒,待 `gen-slot-art` 產圖)。
- ways/cluster 用預設 paytable RTP 偏高,當主玩法前需 `tune-rtp` 重對。
- KG 需使用者手動 `npm install` + 重啟才生效。
- 本機覆寫檔可能殘留尾端 null bytes;build/parse 前已用 `python rstrip` 清(見 CLAUDE.md)。
