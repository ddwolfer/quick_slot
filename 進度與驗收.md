# QuickSlot — 進度與驗收（2026-06-03）

## 這次做完的

M0 骨架 + M1 payline 可玩，全部驗證通過。一個輕量、config 驅動的本地 PixiJS 8 slot 試玩。

## 怎麼跑（早上驗收用）

```bash
cd Desktop/QuickSlot/template
npm install
npm run dev          # 開 http://localhost:5173
```

按 `SPIN` 或空白鍵轉動。可調押注、開賠付表。目前符號是 placeholder 色塊（美術是 M2）。

估 RTP：

```bash
cd Desktop/QuickSlot
node tools/sim.mjs 500000
```

## 已驗證項目

| 驗證 | 結果 |
|---|---|
| `npx tsc --noEmit`（strict 型別檢查） | ✅ 通過 |
| `vite build`（production bundle） | ✅ 732 modules，2.1s |
| RTP 模擬（50 萬次） | ✅ **94.2%**（目標 95%）|
| 命中率 | 48.7%（約半數轉動有獎）|
| 最大單轉 | 150x（5 scatter）|
| Event 串流格式（2000 次） | ✅ reveal→win→totalWin，位置在格內，金額正確 |
| 中獎金額算式 | ✅ 例：L4 三連 = 8 × 線注0.04 = 0.32；4 scatter = 25 |

## 架構（已落實，與規格書一致）

- **玩法無關核心** `src/core/`（純 JS，前端與 sim 共用）：`rng` / `engine` / `evaluators/payline`。
- **PixiJS 渲染** `src/render/`：`Reels`（錯位停軸＋回彈）、`WinPresenter`（高亮＋連線＋countup）、`eventPlayer`（逐事件 **全程 await**，不會卡 round）。
- **HTML UI** `src/ui/controls.ts`：押注 / 餘額 / SPIN / 賠付表。
- **config 驅動** `game.config.json`：換遊戲主要只改這裡。
- **tumble 掛點預留**：`eventPlayer.ts` 留了 `tumble` case 註解，`evaluators/index.js` 留了 ways/cluster 接口 —— 升級時核心與 payline 不動。

## 下一步（依你早上決定）

- **M2 美術**：用 `tools/gen-art.md` 的範本派 subagent 跑 codex 產圖，換掉色塊。
- **M3 升級**：實作 `evaluators/cluster.js` + `TumbleLayer`，或 `ways.js`。
- **音效**：定案後依規格書 §9.1 填音效清單，用 `@pixi/sound` 接。

## 已知限制

- 符號是 placeholder（色塊＋文字），尚無美術 / 音效 / spine FX。
- 只有 payline；ways / cluster 為接口，呼叫會丟「尚未實作」。
- 本機這個資料夾的檔案覆寫時編輯器可能殘留 null bytes；已清理，若日後手改後 `tsc` 報 `Invalid character`，跑一次 `python -c "..."` 清尾端 \x00 即可（或重存檔）。
